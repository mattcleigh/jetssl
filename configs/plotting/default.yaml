defaults:
  - _self_
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

hydra:
  output_subdir: null
  run:
    dir: .

prefix: jetclass
suffix: frozen
path: /srv/beegfs/scratch/groups/rodem/jetssl/jetssl_fixed
plot_dir: /home/users/l/leighm/jetssl/plots/final
force_recompute: false

models:
  reg: reg
  diff: diff
  flow: flow
  vae: vae
  kmeans: kmeans
  mdm: mdm
