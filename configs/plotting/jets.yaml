defaults:
  - _self_
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

hydra:
  output_subdir: null
  run:
    dir: .

path: /srv/beegfs/scratch/groups/rodem/jetssl/jetssl_finetune2/
plot_dir: plots/
outfile: test

models: #{}
  a: cwola_kmeans
  b: cwola_diff
  c: cwola_flow
  d: cwola_untrained

