wandb:
  _target_: pytorch_lightning.loggers.wandb.WandbLogger
  # offline: True
  id: null
  log_model: False
  tags: ${tags}
  project: ${project_name}
  name: ${network_name}
  save_dir: ${paths.full_path}
  resume: ${full_resume}
