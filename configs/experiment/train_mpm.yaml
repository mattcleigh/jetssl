
# @package _global_

defaults:
  - override /datamodule: jetclass_masked
  - override /model: mpmdiff # jetdino, mpmflow, mpmreg, mpmkmeans, mpmdiff, mpmonlyid

# Allows us to use the partially mappable datasets
trainer:
  max_epochs: 9999999999999
  reload_dataloaders_every_n_epochs: 5
  check_val_every_n_epoch: 5

# Model modifications
project_name: jetssl
network_name: diff # dino, flow, reg, kmeans, diff, onlyid
