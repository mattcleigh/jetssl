
# @package _global_

defaults:
  - override /datamodule: jetclass_masked
  - override /model: jetdino # jetdino, mpmflow, mpmreg, mpmtoken, mpmdiff, mpmonlyid

# Allows us to use the partially mappable datasets
trainer:
  max_epochs: 9999999999999
  reload_dataloaders_every_n_epochs: 5
  check_val_every_n_epoch: 5

# Model modifications
project_name: jetssl
network_name: dino # dino, flow, reg, token, diff, onlyid
