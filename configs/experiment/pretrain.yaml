# @package _global_

defaults:
  # Uncomment which tasks you want to run
  ######################
  # - /model/tasks/id
  # - /model/tasks/reg
  - /model/tasks/kmeans
  # - /model/tasks/diff
  # - /model/tasks/flow
  - /model/tasks/probe # Keep this on for benchmarking
  ######################
  - override /datamodule: jetclass_masked
  - override /model: mpm

trainer:
  max_epochs: 1000
  val_check_interval: 5000

# Model modifications
project_name: jetssl2
network_name: kmeans2
