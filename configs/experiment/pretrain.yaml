# @package _global_

defaults:
  # Uncomment which tasks you want to run
  ######################
  # - /model/tasks/id
  # - /model/tasks/reg
  - /model/tasks/kmeans
  # - /model/tasks/diff
  # - /model/tasks/flow
  - /model/tasks/probe # Keep this on for benchmarking
  ######################
  - override /datamodule: jetclass_masked
  - override /model: mpm

# Allows us to use the partially mappable datasets
trainer:
  max_epochs: 1000 # Seems long enough... SCIENCE!
  val_check_interval: 1000 # batches

# Model modifications
project_name: jetssl2
network_name: mpm
